/**
 * @Mobile
 *  should display a topbar with a centered logo
 *  login/logouts at the top right
 *  should have menuItems at bottom of the screen
 *  if there are many, make them swipe-able?
 * @Desktop
 *  should have a topbar logo on left
 *  popovers for menuItems
 * @Navigation
 *  extra component that can sit under topbar full of shortcuts
 */

import TopBar from "./TopBar";

export { TopBar };
